server:
  servlet:
    port: 8080
    contextPath: /api

spring:

  application:
    name: musichoster-api

  datasource:
    driverClassName: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/musichoster
    username: postgres
    password: root

  jpa:
    properties:
      hibernate:
        temp:
          use_jdbc_metadata_defaults: false
    database-platform: org.hibernate.dialect.PostgreSQL9Dialect
